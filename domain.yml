version: "3.1"

intents:
  - general_query

actions:
  - action_search_documents  # Esta linha está faltando no seu domínio

# responses:
#   utter_default:
#     - text: "Processando sua solicitação..."