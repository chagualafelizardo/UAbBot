webhook:
  webhook_rest:
    # Permite que o frontend force intenções
    allow_override_intent: true
    
action_endpoint:
  url: "http://host.docker.internal:5055/webhook"
  timeout: 300  # 5 minutos